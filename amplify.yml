version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    # Vite が出力するフォルダを指定
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*